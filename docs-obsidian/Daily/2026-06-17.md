# 2026-06-17 — Início da Fase 2

## Plano da fase

- Criar base do painel administrativo em `/admin`
- Integrar Firebase Auth com login por e-mail/senha
- Proteger rotas administrativas
- Implementar CRUD de restaurante
- Implementar CRUD de categorias
- Implementar CRUD de produtos
- Adicionar upload de imagens via Firebase Storage
- Adicionar preview do cardápio, copiar link público e QR Code
- Ajustar Firestore Rules para escrita autenticada e isolada por restaurante

## Entrega inicial

- `AuthProvider` client-side com Firebase Auth
- `AdminShell` com navegação e logout
- Rotas iniciais: `/admin/login`, `/admin`, `/admin/restaurante`, `/admin/categorias`, `/admin/produtos`
- Documentação atualizada para marcar a Fase 2 como em desenvolvimento

## Entrega ampliada da Fase 2

- Dashboard simplificado como "Meu cardápio"
- CRUD de informações do restaurante
- CRUD de categorias
- CRUD de produtos
- Upload de imagem de produto via Firebase Storage
- Copiar link público do cardápio
- Gerar e baixar QR Code com `qrcode`
- Firestore Rules por `ownerId`
- Storage Rules para imagens de produto

## Pendência externa

- `npm.cmd run build` passou
- Dry-run/deploy das rules bloqueado por sessão expirada do Firebase CLI
- Próximo passo operacional: executar `firebase login --reauth` e depois publicar `firestore.rules` + `storage.rules`

## Conclusão da fase

- Fase 2 marcada como completa no second brain
- Painel admin simples concluído em código
- Fase 3 passa a ser a próxima fase planejada
- Antes de testar escrita em produção, publicar Firestore Rules e Storage Rules após reautenticar no Firebase CLI
